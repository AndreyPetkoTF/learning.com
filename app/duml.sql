PRAGMA foreign_keys=OFF;
BEGIN TRANSACTION;
CREATE TABLE customer (id INTEGER NOT NULL, name VARCHAR(100) NOT NULL, is_gold BOOLEAN NOT NULL, PRIMARY KEY(id));
CREATE TABLE region(id INTEGER PRIMARY KEY AUTOINCREMENT, name VARCHART(255) NOT NULL UNIQUE, active BOOLEAN NOT NULL DEFAULT TRUE);
INSERT INTO "region" VALUES(1,'region1','TRUE');
CREATE TABLE city(id INTEGER PRIMARY KEY AUTOINCREMENT, name VARCHART(255) NOT NULL UNIQUE, region_id INTEGER NOT NULL,  active BOOLEAN NOT NULL DEFAULT TRUE, FOREIGN KEY(region_id) REFERENCES region(id));
INSERT INTO "city" VALUES(1,'name1',1,'TRUE');
DELETE FROM sqlite_sequence;
INSERT INTO "sqlite_sequence" VALUES('region',1);
INSERT INTO "sqlite_sequence" VALUES('city',1);
COMMIT;
